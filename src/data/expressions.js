const isSunShining = true;
const isNotRaining = false;
const age = 25;

const result = 5 + 1 + 13 + 7;
const isWeatherNice = isSunShining && isNotRaining;
const canEnterClub = age > 21;

const ageDescription = age >= 18 ? "adult" : "child";

export { result, isWeatherNice, canEnterClub, ageDescription };
